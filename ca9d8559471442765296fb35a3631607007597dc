release: v0.7.3 - release trigger fixes, use git notes for release message

ci(dagger-go)

- fix git tag trigger pattern
- use git notes for release message body

ci(tools)

- add a git note when tagging
